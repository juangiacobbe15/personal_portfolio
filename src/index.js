import React from 'react';
import ReactDOM from 'react-dom/client';
import { MiPortfolioApp } from './MiPortfolioApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MiPortfolioApp />
  </React.StrictMode>
);